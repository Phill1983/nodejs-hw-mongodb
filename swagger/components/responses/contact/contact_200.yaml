type: object
description: контакт успішно оновлено
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 200
        message:
          type: string
          example: Contact updated successfully!
        data:
          $ref: '../../schemas/contact.yaml'
